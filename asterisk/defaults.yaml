# -*- coding: utf-8 -*-
# vim: ft=yaml
---
asterisk:
  pkg:
    name: asterisk
  rootgroup: root
  config: '/etc/asterisk'
  service:
    name: asterisk
  subcomponent:
    config: '/etc/asterisk-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
